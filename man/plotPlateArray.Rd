% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPlateArray.R
\name{plotPlateArray}
\alias{plotPlateArray}
\title{Export a grid of \code{plotPlates()} panels to PDF}
\usage{
plotPlateArray(
  plate_tbl,
  fill_var = "colony.size",
  circle_size = 5,
  log_transform = FALSE,
  viridis_palette = "viridis",
  shared_scale = FALSE,
  plate_ids = NULL,
  filename = "plateArray.pdf",
  output_dir = ".",
  width = 16,
  height = 8
)
}
\arguments{
\item{shared_scale}{Logical; if \code{TRUE} apply one min/max colour range across
all panels.}

\item{plate_ids}{Optional vector restricting which \code{plateID}s to plot.}

\item{filename}{Name of the multi-page PDF written to \code{output_dir}.
Default \code{"all_plates.pdf"}.}

\item{output_dir}{Directory into which the PDFs are written.
Created if it does not exist.  Default \code{"."}.}

\item{width, height}{Page dimensions in \strong{inches} (landscape).  Defaults
\verb{16 × 8}.}
}
\value{
(Invisibly) \code{NULL}; called for its side-effects.
}
\description{
\code{plotPlateArray()} is a convenience wrapper that loops over every unique
\verb{(plateID, media)} pair in \code{plate_tbl}, generates a single-page
\code{\link[=plotPlates]{plotPlates()}} panel for that subset, and writes:
}
\details{
\itemize{
\item \strong{One multi-page PDF} (\code{filename}, default \code{"all_plates.pdf"}) that
contains \emph{every} panel, each on its own page
\item \strong{One stand-alone PDF per panel} (named
\code{"plate_<ID>_<media>.pdf"} inside \code{output_dir})
}

The function \strong{inherits all plotting arguments from
\code{\link[=plotPlates]{plotPlates()}}}, passing them straight through, so the appearance of each
panel is identical to calling \code{plotPlates()} directly.
}
