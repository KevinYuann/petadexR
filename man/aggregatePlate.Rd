% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregatePlate.R
\name{aggregatePlate}
\alias{aggregatePlate}
\title{Aggregate biological replicates in plate data}
\usage{
aggregatePlate(plate.tbl, value.cols = c("colony.size", "colony.value"))
}
\arguments{
\item{plate.tbl}{A tibble returned by \code{\link[=readPlate]{readPlate()}} – must contain
\code{plateID}, \code{media}, \code{timepoint}, and \code{constructID}.}

\item{value.cols}{Character vector of column names to summarise.
\strong{Default:} \code{c("colony.size", "colony.value")}.}
}
\value{
A tibble with one row per unique
\{plateID, media, timepoint, constructID\} combination, plus mean,
SD, and N columns for every variable listed in \code{value.cols}.
}
\description{
Collapses replicate wells produced by \code{\link[=readPlate]{readPlate()}} into a single
summary row per experimental condition, defined by the combination of
\strong{plateID, media, timepoint, and constructID}.  The user supplies the
measurement columns to aggregate (defaults are \strong{colony.size} and
\strong{colony.value}); for each of those columns the function appends three
new variables: \verb{<name>_mean}, \verb{<name>_sd}, and \verb{<name>_n}
(number of non-missing replicates contributing to the summary).
}
\details{
\itemize{
\item Non-numeric \code{value.cols} are ignored with a warning.
\item Any \code{value.cols} missing from the input trigger an error.
}
}
\examples{
\dontrun{
tbl <- readPlate("experiment.csv")
agg <- aggregatePlate(tbl, value.cols = c("fluorescence", "OD600"))
}

}
