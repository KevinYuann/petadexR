% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPlates.R
\name{plotPlates}
\alias{plotPlates}
\title{Plot Multiple Plates – ordered by timepoint}
\usage{
plotPlates(
  plate_tbl,
  fill_var,
  circle_size = 5,
  log_transform = FALSE,
  viridis_palette = "viridis",
  shared_scale = FALSE,
  plate_ids = NULL
)
}
\arguments{
\item{shared_scale}{Logical; if \code{TRUE} apply one min/max colour range across
all panels.}

\item{plate_ids}{Optional vector restricting which \code{plateID}s to plot.}
}
\value{
Invisibly returns the \code{gtable} created by
\code{gridExtra::grid.arrange()}.  The grid is printed as a side-effect.
}
\description{
Generates one panel per unique \verb{(plateID, media, timepoint)} and combines
\strong{all} panels into a single near-square grid.
Panels are ordered by \strong{increasing \code{timepoint}}, then \code{plateID}, then
\code{media}, so earlier timepoints always appear first.
}
